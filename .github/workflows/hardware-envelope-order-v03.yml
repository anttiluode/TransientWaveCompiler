name: hardware-envelope-order-v03

on:
  push:
    paths:
      - "transientwave/hardware_envelope_order_v03.py"
      - "transientwave/emulator_v03.py"
      - "transientwave/order_contrast.py"
      - "transientwave/order_benchmarks.py"
      - "docs/HARDWARE_ENVELOPE_ORDER_PREREG_V03.md"
      - ".github/workflows/hardware-envelope-order-v03.yml"
  workflow_dispatch:

jobs:
  envelope:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -e .
      - name: Run frozen order-contrast hardware envelope
        run: python -m transientwave.hardware_envelope_order_v03
      - uses: actions/upload-artifact@v4
        with:
          name: hardware-envelope-order-v03
          path: runs/hardware_envelope_order_v03.json
