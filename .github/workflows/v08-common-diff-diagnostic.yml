name: v08-common-diff-diagnostic

on:
  push:
    branches:
      - agent/tw1a-common-diff-v08
    paths:
      - 'transientwave/circuit_emulator_v08_common_diff.py'
      - 'experiments/v08_common_diff_diagnostic.py'
      - 'docs/CIRCUIT_V08_COMMON_DIFF_DIAGNOSTIC_PREREG.md'
      - '.github/workflows/v08-common-diff-diagnostic.yml'
  workflow_dispatch:

jobs:
  diagnostic:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -e .
      - name: Run frozen v0.8 common-difference diagnostic
        run: python experiments/v08_common_diff_diagnostic.py
      - name: Upload v0.8 diagnostic
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v08-common-diff-diagnostic
          path: v08-common-diff-diagnostic.json
