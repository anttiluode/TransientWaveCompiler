name: v09-seed2400-pair-split

on:
  push:
    branches:
      - agent/tw1a-common-diff-v08
    paths:
      - 'experiments/v09_seed2400_pair_split.py'
      - 'docs/CIRCUIT_V09_SEED2400_PAIR_SPLIT_PREREG.md'
      - '.github/workflows/v09-seed2400-pair-split.yml'
  workflow_dispatch:

jobs:
  diagnose:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -e .
      - name: Run same-draw seed 2400 pair split
        run: python experiments/v09_seed2400_pair_split.py
      - name: Upload pair split
        uses: actions/upload-artifact@v4
        with:
          name: v09-seed2400-pair-split
          path: v09-seed2400-pair-split.json
